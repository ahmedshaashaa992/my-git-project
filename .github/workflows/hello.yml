name: Windows Greeter
on: [push]  # Trigger when you push to GitHub

jobs:
  greet:
    runs-on: windows-latest  # Windows runner!
    steps:
      - run: Write-Output "Hello from Windows! You pushed YAML to Git!"